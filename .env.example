# TypeSafe (Jev) API 키 — https://console.typesafe.ai 에서 발급
TYPESAFE_API_KEY=

# OpenAI API 키 — 답변 생성(GPT)에 사용. GPT_API_KEY 라는 이름도 인식합니다.
OPENAI_API_KEY=

# 선택: 답변 생성 모델 (기본 gpt-5.4)
# OPENAI_MODEL=gpt-5.4

# 선택: legalize-kr 조회 시 GitHub API 제한 완화. 비우면 `gh auth token`을 자동 사용
# GITHUB_TOKEN=
